{
  "name": "@doorstar/production-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only stdio MCP adapters for Doorstar production data and the scoped Nexus knowledge island.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:tenant-runtime": "npm run build && node scripts/buildTenantWoodworkingRuntime.mjs",
    "start": "node dist/index.js",
    "start:nexus": "node dist/nexusBridge.js",
    "verify:nexus-identities": "node scripts/verifyNexusAgentAccess.mjs",
    "dev": "tsx src/index.ts",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "@hono/node-server": "^2.0.5"
  }
}
