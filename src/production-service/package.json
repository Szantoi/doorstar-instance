{
  "name": "@doorstar/production-service",
  "version": "0.1.0",
  "private": true,
  "description": "Üzemi Tábla backend — production board domain API (tasks, kanban, capacity, work-order sheets). Domain vocabulary aligned with joinerytech-platform's spaceos-modules-production (ProductionJob / WorkflowStep / 6-stage workflow) for future merge.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:test": "tsx watch scripts/startTestSchema.ts",
    "build": "tsc -p tsconfig.json && node scripts/copyRuntimeAssets.cjs",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:seed": "tsx prisma/seed.ts",
    "seed:ux-reference": "tsx scripts/seedUxReferenceProject.ts",
    "test": "vitest run",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:migration:m1b": "vitest run --config vitest.migration.config.ts",
    "test:watch": "vitest",
    "verify:planning-input-pack": "tsx scripts/verifyPlanningInputPack.ts",
    "verify:openapi": "tsx scripts/verifyOpenApi.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pino": "^9.6.0",
    "pino-http": "^10.4.0",
    "zod": "^3.24.1"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "pino-pretty": "^13.0.0",
    "prisma": "^6.3.1",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
